// Export the AppRouter type from the main app
// This allows the admin app to use the same tRPC types

// For now, we'll create a placeholder. 
// You'll need to update this with the actual AppRouter export from the main app
export type { AppRouter } from '../../apps/main/src/server/api/root';