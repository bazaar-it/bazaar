# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/src/scripts/node_modules
/src/scripts/*/node_modules
/.pnp
.pnp.js

# testing
/coverage

# database
/prisma/db.sqlite
/prisma/db.sqlite-journal
db.sqlite

# next.js
/.next/
/out/
next-env.d.ts

# production
/build
dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
# do not commit any .env files to git, except for the .env.example file. https://create.t3.gg/en/usage/env-variables#using-environment-variables
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# idea files
.idea

# IDE / Cursor
.cursor/
.cursorrules

# Remotion render outputs only (not source code)
/remotion/out/
/remotion/renders/
/remotion/*.mp4
/remotion/*.png

# Local data & caches
*.sqlite

# Environment files
.env
.env.*
!.env.example          # keep the example stub in the repo

# =============================================================================
# PRODUCTION EXCLUSIONS - Development artifacts excluded from production
# =============================================================================

# Development documentation (keep in development, exclude from production)

# Development logs (keep in development, exclude from production)  
/logs/
/tmp/
*.log

# Development scripts (keep in development, exclude from production)

# Analysis outputs (keep in development, exclude from production)
/analysis/

# Test results and fixtures (keep in development, exclude from production)
/test-results*
/tests/performance/fixtures/

# A2A system (separate project, keep in development, exclude from production)
/src/server/a2a/
/src/agents/
/src/scripts/a2a-test/
/src/scripts/log-agent/
/src/scripts/log-tools/

# Development-only API routes (keep in development, exclude from production)
/src/app/api/a2a/
/src/app/api/debug/
/src/app/api/test/
/src/app/api/test-component/

# Development test pages (keep in development, exclude from production)
/src/app/test/
/src/app/remotion-demo/

# Legacy edit system (keep in development for reference, exclude from production)
/src/app/projects/[id]/edit/

# Temporary files and backups
*.bak
*.backup
*.tmp
*_backup.*
*_stashed_*

# Development database dumps
*.dump
*.sql.backup