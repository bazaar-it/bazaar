# Add this to your .env.local file to enable the ScenePlannerAgent patch:

# Enable the ScenePlannerAgent patch
APPLY_SCENE_PLANNER_PATCH=true

# Set memory limits (if needed)
NODE_OPTIONS="--max-old-space-size=4096"

# You might also need to set the correct model if not already done
DEFAULT_ADB_MODEL=o4-mini
