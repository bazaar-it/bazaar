export const IMAGE_RECREATE_MODE = `
MODE: RECREATE (Reference-only)
- Do NOT display the uploaded images. Use them ONLY as visual reference.
- Recreate the visuals using shapes/text/gradients, matching colors, typography, spacing, and layout.
- Add animation as appropriate for motion graphics.
- For edits: limit changes to the targeted elements; preserve unrelated code.
`;

